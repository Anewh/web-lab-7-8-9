{% extends 'base.html.twig' %}

{% block title %}Edit Answer{% endblock %}

{% block body %}
    <h1>Edit Answer</h1>

    {{ include('answer/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-inline-flex align-items-center mt-3">
        <a href="{{ path('app_question_show', {'id': answer.question.id}) }}" class="btn btn-outline-dark">
            Back to question</a>
        {{ include('answer/_delete_form.html.twig') }}
    </div>

{% endblock %}
