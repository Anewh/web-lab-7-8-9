<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="canonical" href="https://getbootstrap.com/docs/5.1/examples/cheatsheet/">
            <link href="https://getbootstrap.com/docs/5.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
            {# {{ encore_entry_link_tags('app') }} #}
        {% endblock %}

        {% block javascripts %}
            <script src="https://getbootstrap.com/docs/5.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
            {# {{ encore_entry_script_tags('app') }} #}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-dark bg-primary mb-3">
          <div class="container-fluid">
            <a href="{{ path('app_question_index') }}" class="navbar-brand"><strong>Questions And Answers</strong></a>
            <span>
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="btn btn-dark" type="button">Logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-dark me-2" type="button">Login</a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-dark" type="button">Register</a>
                {% endif %}
            </span>
          </div>
        </nav>
        <div class="bd-cheatsheet container bg-body bg-light">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>